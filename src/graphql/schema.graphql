type Product {
  id: ID!
  name: String!
  price: Float!
  inventoryAvailability: Int!
}

type Query {
  hello: String!
  products: [Product!]!
}

type Mutation {
  createProduct(name: String!, price: Float!, inventoryAvailability: Int!): Product
}
